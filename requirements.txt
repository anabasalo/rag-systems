# Runtime dependencies for rag-systems.
#
# All dependencies for every phase live here so a single
# `pip install -r requirements.txt` is enough to run the project from any
# phase forward. Phase 1 only needs config + parsing + chunking + Chroma +
# embeddings; later phases use the rest.

# --- Web layer (Phase 2+) ---
fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.20

# --- Configuration (Phase 1+) ---
pydantic>=2.9
pydantic-settings>=2.6
python-dotenv>=1.0

# --- Document parsing (Phase 1+) ---
pdfplumber>=0.11

# --- Vector store (Phase 1+) ---
chromadb>=0.5

# --- Embeddings (Phase 1+) ---
sentence-transformers>=3.0

# --- LLM provider (Phase 2+) ---
groq>=0.13

# --- Sparse retrieval for hybrid search (Phase 3+) ---
rank-bm25>=0.2.2

# --- Evaluation (Phase 4+) ---
ragas>=0.4
# RAGAS judges metrics with an LLM. We re-use the Groq endpoint via
# its langchain wrapper rather than introducing a second provider.
langchain-groq>=0.2
